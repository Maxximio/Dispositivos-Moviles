<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DispositivosMoviles" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DispositivosMoviles" parent="Base.Theme.DispositivosMoviles">
        <item name="android:fontFamily">@font/roboto_light</item>
        <!--        <item name="android:backgroundTint">@color/blue_dm</item>-->
        <item name="android:textColor">@color/white_dm</item>
        <item name="android:textSize">14sp</item>

    </style>

    <style name="textTitle">
        <item name="android:textColor">@color/title_color_dm</item>
        <item name="android:textSize">48sp</item>
        <item name="android:fontFamily">@font/satisfy</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ButtonsGeneral" parent="@style/Widget.Material3.Button">
        <item name="android:textStyle"></item>
        <item name="android:textSize">12sp</item>
        <item name="android:backgroundTint">@color/blue_dm</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <style name="ButtonsCancel" parent="ButtonsGeneral">
        <item name="android:backgroundTint">@color/yell</item>

    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!--        <item name="android:editTextColor">@android:color/white</item>-->
        <!--This line changes the color of text in Toolbar-->
        <item name="android:textColorPrimary">@color/white_dm</item>
        <!--This line changes the color of icons in toolbar (back, overflow menu icons)-->
        <!--        <item name="android:textColorSecondary">@color/white_dm</item>-->
        <!-- Gets rid of the search icon -->
        <!--        <item name="searchIcon">@drawable/ic_search</item>-->
        <!-- Gets rid of the "underline" in the text -->
        <!--        <item name="queryBackground">@color/white_dm</item>-->
        <!-- Gets rid of the search icon when the SearchView is expanded -->
        <item name="searchHintIcon">@null</item>
        <!--         The hint text that appears when the user has not typed anything -->
        <!--        <item name="queryHint">@string/search_hint</item>-->
    </style>

    <style name="barraBusqueda" parent="Widget.AppCompat.TextView">
        <item name="android:textColorPrimary">@color/white_dm</item>

    </style>

    <style name="ActiveIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/white_dm</item>
    </style>
</resources>